---
const favoritePlaces = [
  {
    name: "Mirador de Cabrera",
    description: "Un hermoso mirador con vistas panorámicas de Cabrera y sus paisajes.",
    imgSrc: "/images/mirador-cabrera.jpg",
    link: "/places/mirador-cabrera"
  },
  {
    name: "Cascada La Playa",
    description: "Una cascada impresionante rodeada de naturaleza pura.",
    imgSrc: "/images/cascada-la-playa.jpg",
    link: "/places/cascada-la-playa"
  },
  {
    name: "Parque Natural de Cabrera",
    description: "Un parque natural con senderos y biodiversidad local.",
    imgSrc: "/images/parque-natural-cabrera.jpg",
    link: "/places/parque-natural-cabrera"
  },
  {
    name: "Iglesia de Cabrera",
    description: "Una iglesia histórica que refleja la arquitectura colonial de la región.",
    imgSrc: "/images/iglesia-cabrera.jpg",
    link: "/places/iglesia-cabrera"
  }
];
---

<section class="favorites-container">
  <h1>Lugares Favoritos en Cabrera</h1>
  <p class="intro">Explora algunos de los sitios turísticos más destacados en Cabrera, cada uno con su encanto único.</p>

  <div class="places-list">
    {favoritePlaces.map((place) => (
      <div class="place-card">
        <img src={place.imgSrc} alt={`Imagen de ${place.name}`} class="place-image" />
        <h2 class="place-name">{place.name}</h2>
        <p class="place-description">{place.description}</p>
        <a href={place.link} class="details-link">Ver más detalles</a>
      </div>
    ))}
  </div>
</section>

<style>
  :root {
    --bg-color: #f5f5f5;
    --text-color: #333;
    --highlight-color: #3cb371;
    --card-bg: #fff;
    --card-shadow: rgba(0, 0, 0, 0.15);
  }

  .favorites-container {
    background-color: var(--bg-color);
    color: var(--text-color);
    padding: 2rem;
    min-height: 100vh;
    text-align: center;
  }

  h1 {
    font-size: 1.8rem;
    margin-bottom: 0.5rem;
    color: var(--highlight-color);
  }

  .intro {
    font-size: 1rem;
    margin-bottom: 2rem;
  }

  .places-list {
    display: grid;
    gap: 1.5rem;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  }

  .place-card {
    background: var(--card-bg);
    border-radius: 10px;
    box-shadow: 0px 4px 8px var(--card-shadow);
    padding: 1rem;
    text-align: left;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .place-card:hover {
    transform: scale(1.03);
    box-shadow: 0px 6px 12px var(--card-shadow);
  }

  .place-image {
    width: 100%;
    border-radius: 8px;
    margin-bottom: 1rem;
  }

  .place-name {
    font-size: 1.3rem;
    color: var(--highlight-color);
  }

  .place-description {
    font-size: 0.9rem;
    margin: 0.5rem 0 1rem;
    color: #555;
  }

  .details-link {
    color: var(--highlight-color);
    font-weight: bold;
    text-decoration: none;
  }

  .details-link:hover {
    text-decoration: underline;
  }
</style>
